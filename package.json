{
  "name": "connectfour",
  "version": "1.0.0",
  "description": "A game of connect four",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "build:url": "parcel build src/index.html --public-url /",
    "git-master": "git push -u origin master",
    "git-page": "git subtree push --prefix dist origin gh-pages"
  },
  "author": "Fraser Sabine",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.12.4",
    "sass": "^1.23.0"
  }
}